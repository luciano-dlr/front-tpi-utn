// Punto de entrada — Food Store Frontend
// Esta aplicación usa Vite multi-page; cada página HTML es su propio entry point.
// Este archivo existe por compatibilidad con la estructura del proyecto.
// No requiere lógica aquí porque cada página carga su propio módulo vía <script type="module">.
console.log('Food Store — Frontend Web v1.0');
